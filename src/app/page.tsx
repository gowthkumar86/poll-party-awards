export default function ApiServerPlaceholderPage() {
  return (
    <main style={{ fontFamily: "sans-serif", padding: "2rem" }}>
      <h1>Poll backend is running</h1>
      <p>Use the Vite frontend at http://localhost:8080 to access the app.</p>
    </main>
  );
}
